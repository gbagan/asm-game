{
  "dependencies": {
    "purescript": "^0.14.1",
    "spago": "^0.20.2"
  },
  "scripts": {
    "build": "spago bundle-app --to dist/bundle.js",
    "deploy:test": "scp -r dist/* gbagan@connect.liris.cnrs.fr:~/asmtest/"
  }
}
